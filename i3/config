# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# pacman -S gucharmap font-manager ttf-hack-nerd
font pango:{{i3_font_name}},Hack Nerd Font {{i3_font_size}}

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec_always --no-startup-id ~/.screenlayout/{{screenlayout}} && feh --bg-fill ~/Pictures/wallpapers/{{wallpaper}}

exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --ignore-sleep -- xscreensaver-command -lock


# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id "amixer set Master 2%+ && volnoti-show $(amixer get Master | grep -Po '[0-9]+(?=%)' | head -1)"
bindsym XF86AudioLowerVolume exec --no-startup-id "amixer set Master 2%- && volnoti-show $(amixer get Master | grep -Po '[0-9]+(?=%)' | head -1)"
bindsym XF86AudioMute exec --no-startup-id "amixer set Master toggle && if amixer get Master | grep -Fq '[off]'; then volnoti-show -m; else volnoti-show $(amixer get Master | grep -Po '[0-9]+(?=%)' | head -1); fi"
bindsym XF86AudioPlay exec "$HOME/bin/mplayer.sh play-pause"
bindsym XF86AudioStop exec "$HOME/bin/mplayer.sh stop"
bindsym XF86AudioPrev exec "$HOME/bin/mplayer.sh previous"
bindsym XF86AudioNext exec "$HOME/bin/mplayer.sh next"
bindsym XF86Calculator exec --no-startup-id kcalc

bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
#tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty
bindsym $mod+Shift+Return exec --no-startup-id xfce4-appfinder

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id "rofi -show drun -modi drun,calc,run -no-show-match -no-sort -show-icons"
bindsym $mod+c exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shippina
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

bindsym $mod+Control+l exec --no-startup-id "xscreensaver-command -lock"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# cycle focus
bindsym Mod1+Tab focus next
bindsym Mod1+Shift+Tab focus prev

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in vertical orientation
bindsym $mod+Shift+v split h

# split in horizontal orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus floating enable, resize set 1600px 1000px, move to position center, move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show
# bindsym $mod+m [class="thunar"] scratchpad show; move position center

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child


set $out_main {{display_main}}
set $out_second {{display_second}}
set $out_third {{display_third}}

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
{{#if enable_ws_names}}
set $ws1 "1: www"
set $ws2 "2: others"
set $ws3 "3: term"
set $ws4 "4: files"
set $ws5 "5: reading"
set $ws6 "6: stuff"
set $ws7 "7: more stuff"
set $ws8 "8: email"
set $ws9 "9: music"
set $ws10 "10: chat"
set $ws11 "11"
set $ws12 "12"
set $ws13 "13"
{{else}}
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $ws11 "11"
set $ws12 "12"
set $ws13 "13"
{{/if}}

workspace $ws1 output $out_main
workspace $ws2 output $out_second
workspace $ws3 output $out_main
workspace $ws4 output $out_main
workspace $ws5 output $out_main
workspace $ws6 output $out_second
workspace $ws7 output $out_main
workspace $ws8 output $out_second
workspace $ws9 output $out_second
workspace $ws10 output $out_second
workspace $ws11 output $out_third
workspace $ws12 output $out_third
workspace $ws13 output $out_third

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindsym $mod+Control+1 workspace number $ws11
bindsym $mod+Control+2 workspace number $ws12
bindsym $mod+Control+3 workspace number $ws13

# mx keys keypad
bindsym KP_Home workspace number $ws7
bindsym KP_Up workspace number $ws8
bindsym KP_Prior workspace number $ws9
bindsym KP_Left workspace number $ws4
bindsym KP_Begin workspace number $ws5
bindsym KP_Right workspace number $ws6
bindsym KP_Insert workspace number $ws10
bindsym KP_End exec --no-startup-id mousepad
bindsym KP_Down exec --no-startup-id thunar
bindsym KP_Next exec --no-startup-id calibre

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
bindsym $mod+Shift+Control+1 move container to workspace number $ws11
bindsym $mod+Shift+Control+2 move container to workspace number $ws12
bindsym $mod+Shift+Control+3 move container to workspace number $ws13

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+e exec rofi -show p -modi p:rofi-power-menu

# resize window (you can also use the mouse for that)
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode
  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  bindsym j resize shrink width 10 px or 10 ppt
  bindsym k resize grow height 10 px or 10 ppt
  bindsym l resize shrink height 10 px or 10 ppt
  bindsym semicolon resize grow width 10 px or 10 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 10 px or 10 ppt
  bindsym Down resize grow height 10 px or 10 ppt
  bindsym Up resize shrink height 10 px or 10 ppt
  bindsym Right resize grow width 10 px or 10 ppt
  # back to normal: Enter or Escape or $mod+r
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"
bindsym $mod+x focus output right
bindsym $mod+Shift+x [urgent=latest] focus

default_border normal 5

# class                 border  backgr. text    indicator child_border
client.focused          #13a10e #13A10E #000000 #13a10e   #13a10e
client.focused_inactive #414345 #5f676a #000000 #484e50   #5f676a
client.unfocused        #6f7478 #969799 #ffffff #292d2e   #222222
client.urgent           #2f343a #fc0303 #ffffff #fc0303   #fc0303
client.placeholder      #000000 #0c0c0c #000000 #000000   #0c0c0c
client.background       #ffffff

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    position top
    status_command i3blocks -c ~/.config/i3blocks/primary
    tray_output $out_main
    output $out_main
    colors {
        background #dcdad5
        statusline #000000
        separator #666666

        focused_workspace  #585959 #5e5e5e #d7dbdb
        active_workspace   #333333 #5f676a #000000
        inactive_workspace #8f8e8d #c9c8c3 #3e3f40
        urgent_workspace   #2f343a #fc0303 #ffffff
        binding_mode       #2f343a #fc0303 #ffffff
    }
}

# bar {
#     position top
#     output $out_second
#     tray_output none
#     status_command i3blocks -c ~/.config/i3blocks/second
#     colors {
#         background #dcdad5
#         statusline #000000
#         separator #666666
#
#         focused_workspace  #585959 #5e5e5e #d7dbdb
#         active_workspace   #333333 #5f676a #000000
#         inactive_workspace #8f8e8d #c9c8c3 #3e3f40
#         urgent_workspace   #2f343a #fc0303 #ffffff
#         binding_mode       #2f343a #fc0303 #ffffff
#     }
# }

# bar {
#     position top
#     output $out_third
#     tray_output none
#     status_command i3blocks -c ~/.config/i3blocks/third
#     colors {
#         background #dcdad5
#         statusline #000000
#         separator #666666
#
#         focused_workspace  #585959 #5e5e5e #d7dbdb
#         active_workspace   #333333 #5f676a #000000
#         inactive_workspace #8f8e8d #c9c8c3 #3e3f40
#         urgent_workspace   #2f343a #fc0303 #ffffff
#         binding_mode       #2f343a #fc0303 #ffffff
#     }
# }

# custom options
focus_follows_mouse no
# workspace_auto_back_and_forth yes

# custom bindings
bindsym Print exec --no-startup-id flameshot full
bindsym Shift+Print exec --no-startup-id flameshot gui


bindsym $mod+t exec --no-startup-id thunar

# for delux split kb
bindsym F12 exec --no-startup-id flameshot full
bindsym Shift+F12 exec --no-startup-id flameshot gui

bindsym $mod+Shift+a move container to output left; focus output $out_main
bindsym $mod+Shift+s move container to output right; focus output $out_second

# bind right alt for back and forth workspace switcher
bindsym Alt_R workspace back_and_forth

# speaker
bindsym KP_Divide exec pacmd set-default-sink "alsa_output.pci-0000_28_00.4.analog-stereo"
# headset
bindsym KP_Multiply exec pacmd set-default-sink "alsa_output.usb-SteelSeries_SteelSeries_Arctis_5_00000000-00.analog-chat"
# earphones
bindsym KP_Subtract exec pacmd set-default-sink "bluez_sink.1C_E6_1D_E0_B0_D3.a2dp_sink"

for_window [class="Mechvibes"] move to workspace $ws7
for_window [class="thunderbird"] move to workspace $ws8
for_window [class="protonmail-bridge"] move to workspace $ws8
for_window [class="Spotify"] move to workspace $ws9
for_window [class="Slack"] move to workspace $ws10
for_window [class="Pidgin"] move to workspace $ws7
for_window [class="MainTerm"] move to workspace $ws3
for_window [class="calibre"] move to workspace $ws5
# for yad calendar
for_window [class="Yad"] floating enable

for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
assign [window_role="GtkFileChooserDialog"] output $out_main
for_window [window_role="GtkFileChooserDialog"] floating enable, resize set 1200px 800px, move to position center
for_window [window_role="pop-up"] floating enable, resize set 1000px 800px, move to position center
for_window [class="Xfce4-appfinder"] floating enable, resize set 1200px 800px, move to position center
for_window [title="Save screenshot"] floating enable, resize set 800px 600px, move to position center


exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec --no-startup-id sleep 5 && picom -b
exec --no-startup-id playerctld daemon
exec --no-startup-id xscreensaver -no-splash
exec --no-startup-id volnoti
exec --no-startup-id 1password --silent
exec --no-startup-id wired
exec --no-startup-id gromit-mpx
exec --no-startup-id lxpolkit
exec --no-startup-id protonmail-bridge
exec --no-startup-id thunderbird
exec --no-startup-id $HOME/scripts/notify_sounds.sh
{{#if enable_razer}}
exec --no-startup-id polychromatic-tray-applet
{{/if}}
{{#if enable_mechivibes}}
exec --no-startup-id mechvibes{{#if enable_mechivibes_gpu}} --in-process-gpu{{/if}}
{{/if}}
{{#if enable_vpn}}
exec --no-startup-id mullvad connect && mullvad-vpn
{{/if}}
exec --no-startup-id alacritty --class MainTerm --command tmux
# exec --no-startup-id spotify
# exec --no-startup-id slack
